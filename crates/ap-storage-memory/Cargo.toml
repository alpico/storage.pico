[package]
name = "ap-storage-memory"
description = "In-memory caches for ap-storage-* crates."
version = "0.1.0"
edition = "2021"
license = "MIT"
homepage = "https://github.com/alpico/storage.pico"

[dependencies]
ap-storage = { path="../ap-storage" }
